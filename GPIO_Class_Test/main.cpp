#include "GPIO.h"
#include "System.h"


int main(void)
{

    while (1)
    {
        /*
         * Template for debouncing contacts:
         * if (button pressed)
         * {
         *     delay(50ms);
         *     while (button pressed);
         *     do something;
         *     delay(50ms);
         * }
         */
		 
    }
}
